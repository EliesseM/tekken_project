{# templates/registration/confirmation.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}
  Confirmation requise
{% endblock %}

{% block body %}
  <div class="min-h-screen flex items-center justify-center bg-black text-white p-8">
    <div class="bg-gray-800 p-10 rounded-2xl shadow-xl border border-yellow-500 text-center max-w-md w-full">
      <h1 class="text-3xl font-extrabold text-yellow-400 mb-4">📧 Vérifie ta boîte mail</h1>
      <p class="text-gray-300 mb-3">Un lien de confirmation a été envoyé à ton adresse.</p>
      <p class="text-sm text-gray-400">Clique dessus pour activer ton compte.</p>
      <a href="{{ path('app_login') }}" class="inline-block mt-6 bg-red-600 hover:bg-yellow-400 text-white hover:text-black font-semibold py-2 px-4 rounded-lg transition">🔑 Retour à la connexion</a>
    </div>
  </div>
{% endblock %}
